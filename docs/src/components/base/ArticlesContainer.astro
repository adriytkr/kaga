---
import {getCollection} from 'astro:content';

import ArticleList from './ArticleList.astro';

const docs=await getCollection('docs');
const articles=docs.filter(doc=>doc.data.isArticle);
---

<ArticleList articles={articles}/>
